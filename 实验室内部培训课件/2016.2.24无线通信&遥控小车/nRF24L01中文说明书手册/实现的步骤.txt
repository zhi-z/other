操作步骤：

    1.模块初始化：
	初始状态时，CE、SCK引脚均拉低，CSN拉高，SPI初始完成。
	随后分别设置SPI接收，发送的地址。设置工作频道、数据长度、发射频率等
	信息。模块随即进入等待状态。

    2.设置工作模式：
	拉低CE设置为StandBy I 模式，配置SPI读写寄存器，随后拉高CE，
	短暂延时。配置好工作模式（所有工作模式及相应寄存器位详见数据手册）。

    3.发送：打包好数据，通过NRF24L01_TxPacket（）函数发送数据包；
      接收：通过NRF24L01_RxPacket（）等待数据的到来。